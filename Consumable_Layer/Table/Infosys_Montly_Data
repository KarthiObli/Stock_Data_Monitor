CREATE TABLE IF NOT EXISTS "Consumable_Layer"."Infosys_Monthly_Data"
(
    "Date" date NOT NULL,
    "Monthly_Label" character varying COLLATE pg_catalog."default" NOT NULL,
    "Price" numeric(7,2) NOT NULL
);
